services:
  api:
    ports:
      - "8000:8000"
    environment:
      ALLOWED_ORIGINS: "*"

  docs:
    ports:
      - "8080:80"
    build:
      args:
        PUBLIC_API_BASE: "http://localhost:8000"
