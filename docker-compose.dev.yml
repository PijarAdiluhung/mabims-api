services:
  api:
    ports:
      - "8000:8000"
    environment:
      ALLOWED_ORIGINS: "*"

  docs:
    ports:
      - "8080:80"
    build:
      args:
        PUBLIC_API_BASE: "http://localhost:8000"

  docs-ssr:
    environment:
      INTERNAL_API_BASE: "http://api:8000"
